---
import Grid from "./@common/Grid.astro";
import BackgroundForm1 from "./BackgroundForm1.astro";

interface Props {
  title: string;
  subtitle: string;
  showBackgroundForm?: boolean;
  class?: string;
}

const { title, subtitle, class: cssClasses } = Astro.props;

const { showBackgroundForm = true } = Astro.props;
const backgroundFormCSSClasses = showBackgroundForm
  ? [
      "overflow-hidden",
      "xl:mt-[-180px]",
      "xl:mb-[-287px]",
      "xl:pt-[273px]",
      "xl:pb-[427px]",
    ]
  : [];
---

<section
  class:list={[
    backgroundFormCSSClasses,
    cssClasses,
    {
      "mt-[90px]": !showBackgroundForm,
    },
  ]}
>
  <div class="relative container">
    {
      showBackgroundForm && (
        <BackgroundForm1 class="absolute z-[-1] top-[-125px] right-[-540px] rotate-[-31deg]" />
      )
    }
    <Grid class="gap-y-6">
      <h1 class="xl:col-start-2 xl:col-span-full text-display-medium">
        {title}
      </h1>
      {
        subtitle && (
          <h2 class="xl:col-start-2 xl:col-span-6 text-body-extra-large">
            {subtitle}
          </h2>
        )
      }
    </Grid>
  </div>
</section>
