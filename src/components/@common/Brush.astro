---
import Image from "../@common/Image.astro";

interface Props {
  id: string;
}

const { id, ...props } = Astro.props;
---

<Image
  srcSet={[
    `images/brush${id}.png`,
    `images/brush${id}.avif`,
    `images/brush${id}.webp`,
  ]}
  {...props}
/>
