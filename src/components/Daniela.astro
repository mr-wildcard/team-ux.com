---
import Picture from "./@common/Picture.astro";
import Daniela from "../images/daniela.png";

const { width, height, class: cssClasses, alt = "" } = Astro.props;
---

<Picture
  src={Daniela}
  alt={alt}
  class:list={["rounded-t-full", "rounded-b-full", cssClasses]}
  loading="eager"
/>
