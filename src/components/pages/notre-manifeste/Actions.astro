---
import data from "../../../data/notre-manifeste/actions.yaml";
import Grid from "../../@common/Grid.astro";
import Brush from "../../@common/Brush.astro";
---

<section class="container">
  <Grid>
    <h2
      class="col-span-full lg:col-start-2 lg:col-span-6 mb-8 lg:mb-10 text-headline-extra-large lg:text-display-small"
    >
      {data.title}
    </h2>
    <p
      class="col-span-full lg:col-start-2 lg:col-span-8 text-body-large lg:text-body-extra-large mb-10 lg:mb-20"
    >
      {data.introduction}
    </p>
    <div class="col-span-full lg:col-start-2 lg:col-span-10">
      <ul class="lg:columns-2 lg:gap-8 space-y-2.5 lg:space-y-5">
        {
          data.actions.map((action) => {
            return (
              <li class="break-inside-avoid	flex flex-col gap-5 p-5 lg:p-10 bg-surface7 rounded-[26px]">
                <h3 class="text-headline-medium lg:text-headline-medium-bold">
                  {action.title}
                </h3>
                <p class="lg:mr-10 text-body-medium">{action.content}</p>
              </li>
            );
          })
        }
      </ul>
    </div>
  </Grid>
</section>
